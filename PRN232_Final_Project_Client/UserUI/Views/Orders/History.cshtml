
@using System.Linq

@* @{
    ViewData["Title"] = "History";
    var orderItems = Model as List<Order>;
} *@

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BakeryShop</title>

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/Cart.css" asp-append-version="true">

    <!-- DataTables CSS -->
    <link rel="stylesheet" href="https://cdn.datatables.net/2.2.1/css/dataTables.dataTables.css" />
</head>

<body>
    <div class="hero" style="color: #fff">
        <h1 class="header-cart text-center">HISTORY</h1>
        <div class="breadcrumb-container d-flex justify-content-center">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "Index" && ViewContext.RouteData.Values["controller"].ToString() == "Home" ? "active" : "")"
                           asp-controller="Home" asp-action="Index">Home</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page" style="color: #fff">Order</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="container">
       @*  @if (orderItems == null || !orderItems.Any())
        {
            <div class="alert alert-info mt-5">
                You don't have any orders yet.
            </div>
        }
        else
        {
            <div class="table-responsive mt-5">
                <table class="table table-striped table-hover" id="orderTable">
                    <thead class="table-dark">
                        <tr>
                            <th>Order Date</th>
                            <th>Total Amount</th>
                            <th>Shipping Address</th>
                            <th>Status</th>
                            <th>Payment Method</th>
                            <th>Payment Status</th>
                            <th>Items</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in orderItems)
                        {
                            <tr>
                                <td>@(order.OrderDate?.ToString("MM/dd/yyyy") ?? "N/A")</td>
                                <td>@order.TotalAmount.ToString("C")</td>
                                <td>@order.ShippingAddress</td>
                                <td>@(order.OrderStatus ?? "Pending")</td>
                                <td>@(order.PaymentMethod ?? "N/A")</td>
                                <td>@(order.PaymentStatus ?? "Pending")</td>
                                <td>@order.OrderDetails.Count items</td>
                                <td>
                                    <a asp-action="HistoryDetail" asp-route-id="@order.OrderId" class="btn btn-sm btn-primary">
                                        <i class="bi bi-eye"></i> Details
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        } *@
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/2.2.1/js/dataTables.js"></script>

    <!-- Initialize DataTables -->
    <script>
        $(document).ready(function () {
            $('#orderTable').DataTable();
        });
    </script>
</body>
